apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-rbac-cm
  namespace: argocd

data:
  policy.product-c.csv: |
    p, role:prodCgroup, applications, get, product-c-project/product-c-apps, allow
    p, role:prodCgroup, applications, sync, product-c-project/product-c-apps, allow
    p, role:prodCgroup, applications, delete, product-c-project/product-c-apps, deny

    g, prodCgroup, role:prodCgroup