apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
  name: argocd-rbac-cm
  namespace: argocd

data:
  policy.prod.csv: |
    p, role:prod, applications, get, team-project/prod-apps, allow
    p, role:prod, applications, sync, team-project/prod-apps, allow

    p, role:prod, applications, get, team-project/qa-apps, allow
    
    p, role:prod, applications, get, team-project/dev-apps, allow
    p, role:prod, applications, sync, team-project/dev-apps, allow
    p, role:prod, applications, delete, team-project/dev-apps, allow

    g, prod, role:prod